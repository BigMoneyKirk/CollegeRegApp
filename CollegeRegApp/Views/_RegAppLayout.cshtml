<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>@ViewBag.Title</title>
    @Styles.Render("~/Content/css")
</head>
<body>
    <nav class="navbar navbar-inverse">
            <ul class="nav navbar-nav">
                <li>
                    @Html.ActionLink("Home", "Home", "RegApp")
                </li>

                <li>
                    @Html.ActionLink("Add Student", "AddStudent", "RegApp")
                </li>

                <li>
                    @Html.ActionLink("List of Courses", "ListOfCourses", "RegApp")
                </li>

                <li>
                    @Html.ActionLink("List of Majors", "ListOfMajors", "RegApp")
                </li>

                <li>
                    @Html.ActionLink("Login/Sign Out", "Login", "RegApp")
                </li>
            </ul>
    </nav>

    <div>
        @RenderBody()
    </div>
</body>
</html>
