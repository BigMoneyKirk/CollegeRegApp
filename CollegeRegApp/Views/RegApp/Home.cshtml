@model Student

@{
    ViewBag.Title = "Home";
}

<div>
    <h3><u>@Model.Firstname's Courses - ID: @Model.Id</u></h3>
    <h5>You are a @ViewData["full"] student</h5>
    
    <p>No courses yet</p>

    <h3><u>Register</u></h3>
  @*  @Html.DropDownList("Courses", "Select course to add:") *@
</div>